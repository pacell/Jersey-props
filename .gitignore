__pycache__/
*.pyc
# Derived geocoding cache (rebuilt on demand; can be large)
data/geocode_cache.json
.venv/
venv/
.DS_Store
# Derived listing image/coords cache
data/source_meta_cache.json
# Heavy downloaded brochure PDFs (archive only; site links remote URLs)
docs/brochures/
